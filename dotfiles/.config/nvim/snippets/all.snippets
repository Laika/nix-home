snippet date "Date"
  `strftime("%Y-%m-%d")`

snippet now "Timestamp"
  `strftime("%Y-%m-%dT%H:%M:%S%:z")`

snippet apt-get "Docker apt-get template"
  RUN apt-get update && apt-get install -y --no-install-recommends \
    ${1:packages} \
  && apt-get clean \
  && rm -rf /var/lib/apt/lists/*
